apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: pdf-worker-monitor
  namespace: pdf-reports
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: pdf-worker
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
