apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: report-service-monitor
  namespace: pdf-reports
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: report-service
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
